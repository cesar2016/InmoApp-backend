{
    "$schema": "https://railway.app/v1.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksConfig": {
            "variables": {
                "NIXPACKS_PHP_EXTENSIONS": "ctype, curl, dom, fileinfo, filter, hash, mbstring, openssl, pcre, pdo, session, tokenizer, xml, gd, zip",
                "NIXPACKS_NODE_VERSION": "22"
            }
        }
    },
    "deploy": {
        "startCommand": "sh -c \"php artisan migrate --force && php artisan optimize && exec php artisan serve --host=0.0.0.0 --port=$PORT\""
    }
}
